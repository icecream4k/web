2021.08.26 -
1.父子组件传递是由函数return 对应上子组件的class名,子组件通过this.props.xxx{ 传递名 }来进行接收

2.在组件里面使用onClick进行函数操作的时候,推荐使用箭头函数来规避this的指向问题,onClick = {()=>{}},如果进行函数操作,
例如onClick = {()=>{alert('666')}}时,不要免去箭头函数,写成onClick={alert('666')},这样会导致组件在每次渲染的时候都会执行内部函数而不是由
点击来进行触发

